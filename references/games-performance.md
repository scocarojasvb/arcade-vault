# Rendimiento de render por juego

Memoria del subagente `game-performance` (`.claude/agents/game-performance.md`). Un juego marcado
`completo` en la columna Estado **no se vuelve a tocar en corridas futuras** salvo que el usuario
lo nombre explícitamente en su prompt. `sin medir` significa que el profiler no estuvo disponible
en esa corrida — nunca se marca `completo` sin una medición final real.

| Juego       | Estado   | classic | neon | retro | avg frame base → final (ms)                                                         | Técnica / notas                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Última corrida |
| ----------- | -------- | ------- | ---- | ----- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------- |
| `frogger`   | completo | ok      | ok   | ok    | 18.17 → 18.17 (SPEC 12)                                                             | roundRect nativo + sprites de glow (SPEC 12)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 2026-08-21     |
| `tetris`    | completo | n/a     | n/a  | n/a   | 39.50 → 38.53 (= dentro del ruido; camino de dibujo -23.5%)                         | Fondo + grilla horneados una sola vez a un canvas offscreen y compuestos con un `drawImage` por frame (antes 28 × `beginPath`/`moveTo`/`lineTo`/`stroke` = 112 llamadas nativas por frame, geometría constante), y celdas agrupadas por color en buckets reutilizados (`collectCell`/`flushCells`) para escribir `fillStyle` ~11 veces y `globalAlpha` ~2 en vez de 139 + 138 por frame. Total de llamadas nativas de canvas por frame con tablero lleno: **530 → 155 (-71%)**; micro-benchmark A/B del patrón exacto en el mismo Chrome: 0.140 → 0.107 ms/frame (**-23.5%**) con salida **pixel-idéntica** (0 de 720.000 bytes distintos). El frame time no mejora de forma medible porque el techo del entorno es externo al juego (React/Next + compositing del CRT + la máquina con load 12 por corridas paralelas): el dibujo de tetris era ya solo ~0.14 ms de un frame de ~35 ms. `useState(scale)` se dejó como está: lo dispara el `ResizeObserver`, no el loop. Sin skins: tetris ignora la prop `skin` (`skin-designer` lo tiene `pendiente`), así que las 3 skins renderizan idéntico y se midió una sola configuración visual × 2 estados de tablero (vacío / 8 filas al 75%).                                                                                                                                                      | 2026-08-21     |
| `asteroids` | completo | ok      | ok   | ok    | 26.60 → 26.51 (= , limitado por composición; costo del loop 0.294 → 0.221 ms, −25%) | Izado de `fillStyle`/`strokeStyle`/`lineWidth`/`lineJoin`/`shadowBlur`/`shadowColor` fuera de `Bullet/Asteroid/Particle.draw` y del bucle de `drawLifeIcon` a un set por grupo en `draw()`/`drawHUD`; glow omitido cuando `skin.glow === 0` (classic/retro); tabla de 101 colores `rgba` por skin para las partículas (variable de closure, no `useState`); `forEach` → `for` indexado en el camino de dibujo. Escrituras de estilo del ctx2D: 782 → 275 por frame; `draw` (self time, carga fija) 107 → 65 ms/8 s. Se añadió `lineJoin = "miter"` explícito en `PowerUp.draw` para que el izado no cambie las esquinas del marco del power-up.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 2026-08-21     |
| `arkanoid`  | completo | ok      | ok   | ok    | 34.91 → 35.89 (=, ±3% de ruido; JS del juego ~1% del trace)                         | Sin cambios: la medición no señaló ningún antipatrón del catálogo en el camino de dibujo (~0.2 ms/frame, ~77 blits `drawImage` ya horneados con `bakeTintedSheet`). El techo de frame time del entorno es el compositing del CRT (`.game-viewport::after` con `mix-blend-mode: multiply`), compartido por los 5 juegos y parte de la identidad visual — no se tocó.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 2026-08-21     |
| `snake`     | completo | ok      | ok   | ok    | 33.89 → 42.88 (ruido del entorno; camino de dibujo -48%)                            | `drawSnake` reescrito: `forEach` con arrow recreada por frame → `for` indexado, y `fillStyle`/`shadowColor`/`shadowBlur` izados fuera del bucle (el cuerpo comparte color y glow; la cabeza se dibuja antes del bucle para conservar el mismo orden de superposición de halos). Además `ctx.font`/`ctx.textBaseline` fijados una vez al montar el `useEffect` (idiom de `frogger-game.tsx:280-282`); `textAlign` sigue alternando en `draw()` porque sí varía. Medido con serpiente de 400 segmentos (~13× una partida larga) para que el camino de dibujo fuera visible en el perfil: self time de `drawSnake` **21.40 → 2.68 ms (-87%)**, `fillRect` nativo **19.36 → 10.27 ms (-47%)**, costo total del dibujo **0.240 → 0.126 ms/frame (-48%)** en classic; retro 0.225 → ~0.10. **No** se horneó el glow a sprite offscreen: la medición lo descarta explícitamente — retro (`glowHead`/`glowBody` en `0`) no es más rápido que classic/neon a 400 segmentos (0.21 vs 0.23 ms/frame), así que `shadowBlur` no es un costo relevante aquí. Tampoco hay `arcTo` (snake usa `fillRect`). El techo de frame time es externo al juego (compositing del CRT + React/Next dev + Chrome headless compartido con 2 corridas paralelas), por eso el avg frame se mueve con el ruido y no con el fix. Valores de `app/games/snake/skins.ts` sin tocar. | 2026-08-21     |
