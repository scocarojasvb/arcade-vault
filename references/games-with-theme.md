# Juegos con tema (skins) implementado

Memoria del subagente `skin-designer` (`.claude/agents/skin-designer.md`). Un juego marcado
`completo` en la columna Estado **no se vuelve a tocar en corridas futuras** salvo que el
usuario lo nombre explícitamente en su prompt.

| Juego       | Estado    | classic | neon  | retro | Técnica / notas                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Última corrida |
| ----------- | --------- | ------- | ----- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------- |
| `asteroids` | completo  | ok      | ok    | ok    | Paleta vectorial pura (`app/games/asteroids/skins.ts`), sin spritesheet. neon: acento amarillo (`--yellow`) + cyan/magenta/verde, glow `shadowBlur 9`. retro: rampa ámbar 4 tonos `#1a1206/#c04000/#ff7b00/#ffb000`, `glow 0`. Las partículas usan `particleRgb` porque el alpha lo calcula el frame. Repinta al cambiar de skin incluso en pausa/game over vía `redrawRef`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 2026-08-20     |
| `tetris`    | pendiente | falta   | falta | falta | Sin `app/games/tetris/skins.ts`; 3 literales de color en el camino de dibujo + array `COLORS`. `drawNext()` solo corre al fijar pieza: habrá que repintar el preview al cambiar de skin.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 2026-08-20     |
| `arkanoid`  | completo  | ok      | ok    | ok    | Spritesheet completo (`/games/arkanoid/spritesheet-breakout.png`) horneado con `bakeTintedSheet` (`app/games/skin-utils.ts`) en `bakeSheet()`, solo al cargar o al cambiar de skin, nunca por frame. Paleta en `app/games/arkanoid/skins.ts`: solo `bg` + `hudText` + `glow` a mano, el resto es `tint`. neon: acento magenta (`--magenta`, color de catálogo), `bg #0a0a0f`, HUD `#e6e9ff`, glow 10, hoja con `hue-rotate(295deg) saturate(1.5) brightness(1.1)`. retro: `bg #1a1206`, HUD `#ffb000`, hoja `grayscale(1) brightness(1.1)` + `multiply #ffb000`, glow 0. **Desviación aceptada (no "arreglar")**: el `hue-rotate` de neon mueve todos los colores de bloque juntos, así que no caen exactamente en los 4 tokens de acento; se prefiere sobre un tinte plano por color con `source-atop` porque conserva el sombreado interno del sprite. Re-hornea y repinta al cambiar de skin incluso en pausa/game over vía `applySkinRef`.                                                                                                                                                                                                                                                                                                                                                                                                                     | 2026-08-20     |
| `snake`     | completo  | ok      | ok    | ok    | Paleta en `app/games/snake/skins.ts` (`bg`/`head`/`body`/`glowColor`/`glowHead`/`glowBody`/`hudText` + `fruitTint`). neon: acento verde (`--green`, color de catálogo) con cabeza `--yellow`, `bg #0a0a0f`, HUD `#e6e9ff`, glow 12/8. retro: rampa verde tipo Game Boy `#0f380f`/`#306230`/`#8bac0f`/`#9bbc0f`, glow 0. La hoja de frutas (`/games/snake/fruits.png`) se hornea con `bakeTintedSheet` en `ensureFruitsSheet()` — solo al cargar o al cambiar de skin, nunca por frame; la caché es un objeto `const` mutado (`tintedFruits`) y no un `let`, porque el hook de formato convierte esos `let` a `const` y rompía el build. **Desviación aceptada (no "arreglar")**: en neon el `hue-rotate(200deg) saturate(1.6) brightness(1.1)` mueve todos los tonos de las frutas en bloque, así que no caen exactamente sobre los 4 tokens de acento; se prefiere porque conserva el sombreado interno del sprite. Repinta al cambiar de skin incluso en pausa/game over vía `redrawRef`, y también al terminar de cargar la hoja.                                                                                                                                                                                                                                                                                                                               | 2026-08-20     |
| `frogger`   | completo  | ok      | ok    | ok    | Paleta vectorial pura (`app/games/frogger/skins.ts`), sin spritesheet: 21 roles de color + 3 niveles de glow (`glowPlatform` 6 / `glowVehicle` 8 / `glowActor` 10 en classic, 8/10/12 en neon, 0/0/0 en retro). El array módulo-level `ROAD_LANE_COLORS` se eliminó y vive ahora como `vehicles` (5 carriles) en la paleta. neon: acento cyan (`--cyan`, color de catálogo de la seed `20260820000000_seed_frogger_game.sql`) para troncos y nenúfares, verde para rana/tortugas, magenta+amarillo para vehículos (peligro distinguible de plataformas), `bg #0a0a0f`, HUD `#e6e9ff`. retro: rampa ámbar 4 tonos `#1a1206`/`#c04000`/`#ff7b00`/`#ffb000`, glow 0. **Desviación aceptada (no "arreglar")**: los fondos de carril (goal/río/mediana/carretera) son 4 roles distintos que en neon/retro se resuelven con los mismos tokens/tonos de la rampa a distinto alpha (washes sobre `bg`, que se pinta primero), no con tonos nuevos — el juego original ya usaba rgba con alpha (`rgba(0,255,136,0.25)` de la tortuga sumergida), así que es el atributo alterno ya presente en el dibujo. En retro los vehículos repiten tonos entre carriles (`#ff7b00`/`#ffb000`/`#c04000`/`#ffb000`/`#ff7b00`) porque los carriles no necesitan distinguirse entre sí (están en filas separadas). Repinta al cambiar de skin incluso en pausa/game over vía `redrawRef`. | 2026-08-21     |

## Contrato compartido

- `app/games/skins.ts` — `SKIN_IDS` / `SkinId` / `DEFAULT_SKIN` / `SKIN_LABELS` / `isSkinId` /
  `skinStorageKey` (creado el 2026-08-20).
- `app/games/skin-utils.ts` — `SheetTint` / `bakeTintedSheet` (creado el 2026-08-20 al entrar
  `arkanoid`/`snake` al alcance). Horneado a canvas offscreen; el paso `multiply` recorta de vuelta al
  alpha original con `destination-in` porque `multiply` sobre destino transparente inunda el
  fondo de color opaco.
- `RealGameProps.skin?: SkinId` en `app/games/registry.tsx`.
- Selector de 3 chips en `.hud-actions` de `app/juego/[id]/jugar/page.tsx`, persistido por juego
  en `localStorage` (`av_skin_<id>`), estilos `.hud-skin`/`.skin-chip` en `app/globals.css`.
- **Desviación del contrato a conservar**: la skin se lee con `useSyncExternalStore`
  (`subscribeSkin`/`readSkin`/`writeSkin` en `jugar/page.tsx`) en lugar de `useState` +
  `useEffect`, porque `setState` sincrónico dentro de un efecto dispara el error de ESLint
  `react-hooks/set-state-in-effect` (el mismo que ya arrastra `app/auth-context.tsx`). El
  `getServerSnapshot` devuelve `DEFAULT_SKIN`, así que la hidratación sigue siendo segura. No
  "arreglar" esto volviendo al efecto.
