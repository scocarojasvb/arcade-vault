# Rutas con layout móvil verificado

Memoria del subagente `mobile-porter` (`.claude/agents/mobile-porter.md`). Una ruta marcada
`completo` en la columna Estado **no se vuelve a tocar en corridas futuras** salvo que el usuario
la nombre explícitamente en su prompt.

| Ruta                | Estado    | 375px | 768px | landscape | Técnica / notas                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Última corrida |
| ------------------- | --------- | ----- | ----- | --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- |
| `/juego/[id]/jugar` | completo  | ok    | ok    | ok        | HUD en columna + `hud-stat` 2x2 (`flex: 1 1 calc(50% - 24px)`) y `.hud-skin` a fila propia en `@media 520`; `.crt` padding 24→16→10px para devolverle ancho al canvas; `.crt-bottom` con wrap (el título "Frogger" no cabía en una línea); modal compactado (padding/`final`/`input-row` en columna). Canvas sin tocar: los 5 juegos reales son 4:3 y reusan `.asteroids-canvas` (`object-fit: contain`) dentro de `.game-viewport`; Tetris reescala solo por su `ResizeObserver`. **Desviación aceptada (no "arreglar")**: el bloque `@media (orientation: landscape) and (max-height: 520px) and (pointer: coarse)` es la única regla del archivo que discrimina por alto — sin él, HUD + canvas + D-pad no caben con el teléfono acostado (la escalera max-width no aplica porque el ancho pasa a ~700-900px); ahí `.game-viewport` lleva `max-height: 56dvh`. | 2026-08-21     |
| `/biblioteca`       | completo  | ok    | ok    | ok        | `@media 520`: `.av-grid` a `1fr` (auto-fill `minmax(280px,1fr)` desbordaba bajo 280px de contenedor), `.av-filters` en columna con `.av-search { min-width: 0 }`, `.av-hero h1` a `clamp(18px, 6vw, 28px)` (el mínimo de 28px partía "ARCADE VAULT" en dos líneas), `.card` compactada. En `@media (pointer: coarse)`: `.chip` y `.btn` a 44px mínimos y `.card:hover { transform: none }`. `app/components/game-card.tsx` corta el tilt 3D si `(pointer: coarse)` — el tilt se escribe como `style` inline, así que ningún `@media` podía limpiarlo tras un tap. Landscape: flujo de documento normal, sin alto fijo.                                                                                                                                                                                                                                            | 2026-08-21     |
| `/salon-de-la-fama` | pendiente | falta | falta | —         | Tabla de puntajes con grid de columnas fijas en px (`36px 1fr 110px`, `globals.css:1006`) sin override en ningún `@media` — candidata a desbordar en 375px.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 2026-08-20     |
| `/`                 | pendiente | falta | falta | —         | `.home-hero` usa `min-height: calc(100vh - 60px)` (`globals.css:1840`) — único uso de `vh` en vez de `dvh`, puede saltar con la barra de URL móvil.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 2026-08-20     |
| `/juego/[id]`       | completo  | ok    | ok    | ok        | `@media 520`: `.stat-strip` de 3 columnas a filas `1fr` con label/valor en `space-between` (con 3 columnas quedaban ~75px de contenido y "★ ★ ★ ☆ ☆" y las puntuaciones en fuente pixel 16px desbordaban), `.detail-actions` en columna con botones a ancho completo, `.btn.xl` a 12px, `.lb-row` de `36px 1fr 110px` a `30px 1fr auto` (110px no alcanzaban para 7 dígitos). La portada (`.cover-frogger`, `aspect-ratio: 16/10`) ya escalaba bien. Landscape: flujo de documento normal, sin alto fijo.                                                                                                                                                                                                                                                                                                                                                         | 2026-08-21     |
| `/acerca-de`        | pendiente | falta | falta | —         | Sin auditar a fondo todavía (formulario de contacto).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 2026-08-20     |
| `/auth`             | pendiente | falta | falta | —         | Sin auditar a fondo todavía.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 2026-08-20     |

---

**Notas generales de la auditoría inicial (2026-08-20), válidas para todas las filas de arriba:**

- ~~`app/layout.tsx` no exporta `viewport`/`themeColor`~~ — **resuelto el 2026-08-21**: exporta
  `viewport` (`width: device-width`, `initialScale: 1`, `maximumScale: 5`, `themeColor: #0a0a0f`).
  Aplica a las 7 rutas porque el shell es compartido. `maximumScale: 5` es deliberado: no se
  bloquea el zoom manual (accesibilidad), el D-pad ya evita el zoom por gesto con
  `touch-action: none` sobre su área.
- Los targets táctiles viven en un único `@media (pointer: coarse)` **al final de
  `app/globals.css`** — está ahí a propósito, para ganar el desempate por orden de aparición
  frente a las reglas base de igual especificidad. Agregar reglas nuevas ahí, no antes.
- El footer de `app/layout.tsx` usa estilos inline (`style={{...}}`) — un `@media` no puede
  alcanzarlo hasta que se convierta en una clase de `app/globals.css`.
- El escalado del canvas (`.game-viewport { aspect-ratio: 4/3 }` + `object-fit: contain` /
  `.tetris-stage` con `transform: scale()`) ya está resuelto y no debe re-hacerse — es el patrón a
  reutilizar, no un problema.
- `app/components/nav.tsx` ya tiene hamburguesa + drawer `.av-mobile-panel` funcionando a
  `@media (max-width: 840px)` — no re-tocar salvo que un fix de otra ruta lo requiera.
- Ocho breakpoints ad-hoc conviven hoy en `globals.css` (520, 600, 720, 820, 840, 900, 980, 1100);
  el contrato del agente fija 900/720/520 como la escalera a usar en adelante.
