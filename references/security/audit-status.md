# Estado de seguridad por chequeo

Memoria del subagente `security-auditor` (`.claude/agents/security-auditor.md`). Un chequeo marcado
`aceptado` es un desvío consciente documentado en un spec — **no se re-reporta como hallazgo** salvo
que el contexto haya cambiado. `ok` significa verificado con evidencia real en esa corrida; `sin
verificar` significa que la herramienta no estuvo disponible — nunca se marca `ok` sin evidencia.

| Chequeo                                                   | Dominio | Severidad | Estado              | Evidencia / notas                                                                                                                                                                                                                                                                | Última auditoría |
| --------------------------------------------------------- | ------- | --------- | ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| Advisors de seguridad sin findings de RLS/función         | D1      | —         | ok                  | `get_advisors(security)` devuelve solo `auth_leaked_password_protection`; los 4 findings de SPEC 14 (`rls_disabled_in_public` ×2, `anon`/`authenticated_security_definer_function_executable`) ya no aparecen                                                                    | 2026-08-26       |
| RLS habilitado en `games` y `scores`                      | D1      | —         | ok                  | `list_tables`: `rls_enabled: true` en ambas                                                                                                                                                                                                                                      | 2026-08-26       |
| Policies de `games` (solo `SELECT` pública)               | D1      | —         | ok                  | `pg_policies`: única policy `games_select_public` SELECT `{anon,authenticated}` `using (true)`; sin INSERT/UPDATE/DELETE → denegadas por default                                                                                                                                 | 2026-08-26       |
| Policies de `scores` (`SELECT` pública + `INSERT` propio) | D1      | —         | ok                  | `pg_policies`: `scores_select_public` SELECT `{anon,authenticated}` `true`; `scores_insert_own` INSERT `{authenticated}` `with check (user_id = auth.uid())`; sin UPDATE/DELETE                                                                                                  | 2026-08-26       |
| `anon` no puede insertar en `scores`                      | D1      | —         | ok                  | No existe policy de INSERT para `anon` en `pg_policies` → denegado por RLS. Verificado a nivel de policy, no con un insert real (este agente no ejecuta DML)                                                                                                                     | 2026-08-26       |
| `rls_auto_enable()`: SECURITY DEFINER endurecida          | D1      | —         | ok                  | `pg_proc`: `prosecdef=true`, `proconfig=[search_path=pg_catalog]`, `anon_exec=false`, `auth_exec=false`, `public_exec=false`; sigue siendo `returns event_trigger`                                                                                                               | 2026-08-26       |
| Grants de tabla amplios para `anon`/`authenticated`       | D1      | BAJO      | abierto             | `role_table_grants`: `anon` y `authenticated` tienen DELETE,INSERT,REFERENCES,SELECT,TRIGGER,TRUNCATE,UPDATE en `games` y `scores` (default de Supabase). DML queda bloqueado por RLS, pero `TRUNCATE` no está sujeto a RLS (no expuesto vía PostgREST hoy)                      | 2026-08-26       |
| `scores.score` sin `check (score >= 0)`                   | D1      | MEDIO     | abierto             | `pg_constraint` sobre `public.scores`: solo PK + 2 FK, ningún CHECK. Datos actuales sanos (0 negativos, max 2016)                                                                                                                                                                | 2026-08-26       |
| `scores.name` sin tope de longitud en DB                  | D1      | BAJO      | abierto             | `scores.name` es `text` sin CHECK; el tope de 10 chars vive solo en la UI (`app/auth/page.tsx:83`, `app/juego/[id]/jugar/page.tsx:257`). Max real hoy: 19 chars                                                                                                                  | 2026-08-26       |
| `scores.user_id` nullable + filas legacy                  | D1      | —         | aceptado            | 8 de 9 filas con `user_id is null`. SPEC 14 § Out of scope ("Migrar o eliminar puntajes históricos con `user_id: null`") y § Decisions ("No: no se migran puntajes históricos"), mismo razonamiento que SPEC 13                                                                  | 2026-08-26       |
| `supabase/migrations/` vs historial aplicado              | D1      | MEDIO     | abierto             | `supabase_migrations.schema_migrations` tiene `20260729205121_disable_rls_games_and_scores` que no existe en el repo, no tiene la creación de tablas (`0001_games_and_scores.sql` nunca se registró) y los seeds están con versiones distintas a los nombres de archivo del repo | 2026-08-26       |
| Toda policy tiene código que la usa                       | D1      | —         | ok                  | `grep -rn '\.from("' app/` → `scores` (insert en `auth-context.tsx:71`, select en `data/scores.ts:13`), `games` (select en `data/games.ts:121`). Ninguna policy huérfana                                                                                                         | 2026-08-26       |
| `service_role` fuera del código de la app                 | D1/D4   | —         | ok                  | `grep -rn "service_role\|SERVICE_ROLE"` solo matchea `.claude/agents/security-auditor.md`; ni en `app/`, ni en `proxy.ts`, ni en `.env.template`                                                                                                                                 | 2026-08-26       |
| Mínimo de contraseña ≥ 8                                  | D2      | MEDIO     | pendiente-dashboard | Ninguna herramienta MCP conectada lee la config de Auth. SPEC 14 § Decisions lo documenta como ajuste manual de dashboard                                                                                                                                                        | 2026-08-26       |
| Leaked password protection                                | D2      | MEDIO     | pendiente-dashboard | `get_advisors(security)` sigue reportando `auth_leaked_password_protection` (WARN) → sigue desactivado                                                                                                                                                                           | 2026-08-26       |
| Rate limit de signup                                      | D2      | MEDIO     | pendiente-dashboard | Sin señal legible por MCP; sin CAPTCHA/rate limit en código (`app/auth/page.tsx#signup`)                                                                                                                                                                                         | 2026-08-26       |
| `proxy.ts` no gatea rutas (solo refresh de sesión)        | D2      | —         | ok                  | `proxy.ts:28` solo hace `supabase.auth.getUser()`; sin `redirect`/`rewrite`. Cookies propagadas en `setAll` (`proxy.ts:15-21`). `matcher` excluye estáticos e imágenes                                                                                                           | 2026-08-26       |
| `/auth/callback` sin open redirect                        | D2      | —         | ok                  | `app/auth/callback/route.ts:5-16`: solo lee `code`; destinos fijos `${origin}/biblioteca` y `${origin}/auth?error=oauth`; no acepta ningún parámetro `next`/`redirect`                                                                                                           | 2026-08-26       |
| `redirectTo` de OAuth y reset no controlable por atacante | D2      | —         | ok                  | `app/auth/page.tsx:84,121` y `app/auth/recuperar/page.tsx:30` usan `${window.location.origin}` (origen propio), nunca un valor de query string                                                                                                                                   | 2026-08-26       |
| Token de recuperación no se loguea ni persiste            | D2      | —         | ok                  | `app/auth/actualizar-clave/page.tsx` solo usa `getSession()`/`updateUser()`; `grep console.*` fuera de `app/games/` → 0 resultados                                                                                                                                               | 2026-08-26       |
| Validación client-side de contraseña inconsistente        | D2      | BAJO      | abierto             | `app/auth/actualizar-clave/page.tsx:29` exige 6 chars; `app/auth/page.tsx#signup` no valida longitud. Contradice el objetivo de ≥ 8 del checklist                                                                                                                                | 2026-08-26       |
| `AuthProvider` no es control de autorización              | D2      | —         | ok                  | `app/auth-context.tsx:40-88` es `"use client"`; el gate de invitado en `app/juego/[id]/jugar/page.tsx:245` es solo UI y está respaldado por `scores_insert_own` en la DB                                                                                                         | 2026-08-26       |
| Los 3 headers de seguridad del checklist                  | D3      | —         | ok                  | `next.config.ts:6-17` + verificación runtime con `curl -D -` contra `/`, `/salon-de-la-fama` y `/api/contacto`: `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin` en las 3 (incluido el 405)                        | 2026-08-26       |
| CSP / HSTS / `Permissions-Policy` ausentes                | D3      | MEDIO     | aceptado            | Confirmado ausentes en la respuesta runtime. SPEC 14 § Out of scope ("Headers de seguridad adicionales … decisión explícita del usuario de acotar a lo pedido") y § Decisions                                                                                                    | 2026-08-26       |
| `/api/contacto`: `request.json()` sin `try/catch`         | D3      | ALTO      | abierto             | `app/api/contacto/route.ts:7`. Runtime: body `not-json` → 500, body vacío → 500                                                                                                                                                                                                  | 2026-08-26       |
| `/api/contacto`: sin validación de tipo de campos         | D3      | ALTO      | abierto             | `app/api/contacto/route.ts:9` hace `name?.trim()` sobre un valor sin tipar. Runtime: `{"name":123,…}` → 500                                                                                                                                                                      | 2026-08-26       |
| `/api/contacto`: sin tope de longitud por campo           | D3      | MEDIO     | abierto             | `app/api/contacto/route.ts:9-29`: `name`/`email`/`msg` van al cuerpo del correo sin límite de tamaño                                                                                                                                                                             | 2026-08-26       |
| `/api/contacto`: sin rate limiting ni sesión              | D3      | ALTO      | abierto             | `app/api/contacto/route.ts:6`: `POST` público sin throttle → relay de correo a costa de `RESEND_API_KEY`                                                                                                                                                                         | 2026-08-26       |
| Sin sinks de XSS                                          | D3      | —         | ok                  | `grep -rnE "dangerouslySetInnerHTML\|innerHTML\|eval\(\|new Function\("` en `app/` → 0 resultados; `scores.name` se renderiza como texto React en `/salon-de-la-fama`                                                                                                            | 2026-08-26       |
| Integridad de puntajes (`score`/`name` del cliente)       | D3      | MEDIO     | abierto             | `app/auth-context.tsx:68-81` inserta `score`/`name` tal cual llegan; `scores_insert_own` solo valida `user_id = auth.uid()`. Un usuario logueado puede insertar un puntaje arbitrario o un `name` distinto a su `user_metadata.name`                                             | 2026-08-26       |
| Secretos fuera de git                                     | D4      | —         | ok                  | `git ls-files \| grep -i env` → solo `.env.template`; `.gitignore:40-41` cubre `.env*` con `!.env.template`; `.env` local presente pero no trackeado                                                                                                                             | 2026-08-26       |
| Historial git sin `.env` real                             | D4      | —         | ok                  | `git log --all --diff-filter=A --name-only \| grep -i env` → solo `.env.template`                                                                                                                                                                                                | 2026-08-26       |
| `.env.template` sincronizado con `process.env.*`          | D4      | —         | ok                  | Las 3 variables leídas en código (`RESEND_API_KEY`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`) están en el template; `SUPABASE_DB_PASSWORD` es CLI-only y está documentado como tal                                                                     | 2026-08-26       |
| `npm audit --omit=dev`                                    | D4      | MEDIO     | abierto             | 4 high, 0 critical/moderate/low: `sharp <0.35.0` (CVEs libvips), `postcss <=8.5.22`, `nanoid <3.3.18`, `next 9.3.4-canary.0 – 16.3.0-preview.10`. Todas transitivas vía `next@16.2.11`; fix = `next@16.3.3` (no semver-major)                                                    | 2026-08-26       |
| `settings.local.json`: comodines de ejecución/red         | D4      | MEDIO     | abierto             | `.claude/settings.local.json:15,18,19,22,23,24`: `python3 -`, `curl *`, `node *`, `python3 -c ' *`, `pkill *`, `google-chrome *` → ejecución y red arbitrarias pre-aprobadas                                                                                                     | 2026-08-26       |
| `settings.local.json`: entrada obsoleta                   | D4      | BAJO      | abierto             | `.claude/settings.local.json:29`: `shuf … /tmp/claude-1000/…/38fa3603-…/scratchpad/pool.txt`, ruta de una sesión ya extinta                                                                                                                                                      | 2026-08-26       |
| `mcp__supabase__apply_migration` pre-aprobado             | D4      | BAJO      | abierto             | `.claude/settings.local.json:8`: DDL contra la base sin prompt. Este agente nunca lo usa; solo lo necesita `/spec-impl`                                                                                                                                                          | 2026-08-26       |
| Documentación de RLS desactualizada                       | D4      | BAJO      | abierto             | `CLAUDE.md:55` sigue diciendo "two tables, no RLS (deliberate decision from spec 06)"; la base tiene RLS habilitado con 3 policies desde SPEC 14                                                                                                                                 | 2026-08-26       |
